#ifndef GUARD_utils_h
#define GUARD_utils_h

#include <string>

bool is_space(const char &);
bool is_not_space(const char &);
bool is_bracket_open(const char &);
bool is_bracket_close(const char &);
bool is_bracketed(const std::string &);

#endif
